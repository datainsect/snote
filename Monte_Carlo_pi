Monte Carlo 发计算pi

val count = sc.parallelize(1 to n, slices).map { i =>
  val x = random * 2 - 1!
  val y = random * 2 - 1!
  if (x*x + y*y < 1) 1 else 0!
}.reduce(_ + _)

println("Pi is roughly " + 4.0 * count / n)
}

1.parallelize 方法

2.monte carlo 法的逻辑
